# Fudumala

This is the repository for the Fudumala project.
It can be deployed to GitHub Pages using the `deploy` script.

## Deployment

To deploy the site to GitHub Pages:

1. Make sure you have pushed all changes to main.
2. Run `npm run deploy` to push the `production` folder to the `gh-pages` branch.
